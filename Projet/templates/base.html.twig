<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
    {% block stylesheets %}
        {# {{ encore_entry_link_tags('app') }} #}
    {% endblock %}

    {% block javascripts %}
        {# {{ encore_entry_script_tags('app') }} #}
    {% endblock %}
</head>
{% block nav %}
    <nav>
    <ul>
        <li><a href="{{ path('index') }}">Accueil</a></li>
        <li><a href="{{ path('catalogue') }}">Catalogue</a></li>
        {% if app.user == null %}
        <li><a href="{{ path('app_login') }}">Connexion</a>
        <li><a href="{{ path('inscription') }}">Inscription</a>
            {% endif %}
            <!-- Si co -->
            {% if is_granted('ROLE_USER') %}
        <li><a href="{{ path('panier') }}">Panier</a></li>
        {% endif %}
        <!-- Si admin -->
        {% if is_granted('ROLE_ADMIN') %}
            <li><a href="{{ path('admin') }}">Administration</a></li>
        {% endif %}
        <!-- Si super admin -->

        {% if app.user %}
            <li><a href="{{ path('app_logout') }}">Dèconnexion</a></li>
        {% endif %}
    </ul>
    </nav>{% endblock %}
{% if app.user %}
    <div class="mb-3">
        {{ app.user.getNom() }}, {{ app.user.getPrenom() }}
    </div>
{% endif %}
<body>
{% block body %}{% endblock %}
</body>
</html>
