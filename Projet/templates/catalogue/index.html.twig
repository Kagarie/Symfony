{% extends 'base.html.twig' %}

{% block title %}Catalogue{% endblock %}

{% block body %}
    <h1 style="margin: 0 auto; text-align: center; margin-bottom: 2em;">Listes des groupes </h1>
    {{ form_start(form) }}
    <h2>Inscription</h2>
    {{ form_widget(form) }}
    <input type="submit" id='submit' value="S'inscrire">
    {{ form_end(form) }}
    <section>
        {% for g in groupe %}
            <div style="width: 20%; display: inline-block; text-align: center; border: 1px solid; margin-left: 5px ">
                <a href="#">
                    <h2>{{ g.nom }}</h2>
                    <p>Nombre de membres : {{ g.nombreMembre }}</p>
                    <p>Genre : {{ g.genre }}</p>
                    {% if g.cheminLogo is defined %}
                        <img src="images/groupe_logo/{{ g.cheminLogo }}"
                             style="height: 150px;width: 200px; margin: 0 auto 7px"></td>
                    {% endif %}
                </a>
            </div>
        {% endfor %}
    </section>
{% endblock %}
